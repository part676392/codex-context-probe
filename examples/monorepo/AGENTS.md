# Example Root AGENTS.md

## Repository Rules

- Run `pytest` for Python changes.
- Never commit secrets, tokens, cookies, or raw credentials.
- Document release-impacting behavior changes in `docs/`.
